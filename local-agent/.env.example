OPENAI_API_KEY=sk-...

DATA_DIR=./data
CHROMA_DIR=./data/chroma
CHECKPOINT_DIR=./data/checkpoints

EMBEDDING_MODEL=text-embedding-3-small

# Comma-separated or JSON list are both accepted
INDEX_ROOTS=["./"]
